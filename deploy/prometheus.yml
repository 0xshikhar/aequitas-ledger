global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'aequitas-ledger'
    static_configs:
      # Service names from docker-compose.yml (ledger-primary, ledger-follower).
      - targets: ['ledger-primary:6060', 'ledger-follower:6060']
